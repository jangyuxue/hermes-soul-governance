# skills/user-created

Your custom skills. Each skill is a directory with `SKILL.md`.

`maintain.py` scans this directory and registers/unregisters skills in `user_capabilities.json`. See SOUL.md Section 7.2-7.3.

Currently contains:
- `skill-maintenance/` — the maintenance tool itself
